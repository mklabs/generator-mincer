language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mincer' ]; then cd .. && eval "mv $currentfolder generator-mincer" && cd generator-mincer; fi

